# Backlog

- [ ] Decide whether to migrate or invalidate legacy consent records created before repository authority checks.
- [ ] Revisit whether consent `repo_mode` must be bound to the encrypted request mode.
- [ ] Fix root `tsconfig.node.json` type-library configuration if root project-reference typechecking is adopted.

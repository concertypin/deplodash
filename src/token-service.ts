/**
 * Re-export from the new token module location.
 * This file exists for backward compatibility — all implementations
 * live in src/token/.
 */
export { TokenService } from "@/token/service";
export type { TokenRequestResult } from "@/token/service";

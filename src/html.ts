/**
 * @deprecated Migrated to TSX components in src/views/.
 * This stub exists only so that the build does not break on stale imports.
 * Remove once all external references have been migrated.
 */
export {};
